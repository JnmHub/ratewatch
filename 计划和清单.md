# 倍率同步平台：计划和清单

> 本文既是实施计划，也是最终验收清单。完成项必须有代码或测试证据，不以“计划实现”代替。

## 一、产品边界

- [x] 仅做“普通上游 Key → 自有 New API/Sub2API 站点”的单向同步。
- [x] 自有站点必须使用管理员凭证；上游只需要普通 API Key。
- [x] 每条同步任务独立配置固定增减值，允许负数；计算结果小于等于 0 时拒绝写入。
- [x] 同一目标分组只允许一个启用中的写入任务，多个上游账号按有效倍率最大值聚合。
- [x] 不自动重建被删除的目标分组，不处理余额不足，不用失败值覆盖最后成功值。
- [x] 生图不主动探测；New API 普通 Key 日志被动提取并按 request ID 去重真实请求价格，目标无法精确表达时只告警不写入。

## 二、实施阶段

### 1. 工程与安全

- [x] Go 服务、Vue 3 前端、SQLite 持久化的工程骨架。
- [x] 用户注册/登录、租户数据隔离、HMAC 会话令牌。
- [x] 管理员 Key 与上游 Key 使用 AES-256-GCM 加密落库，页面始终脱敏。
- [x] 审计事件只记录异常和发生变化的同步，不记录密钥明文。

### 2. 自有站点接入

- [x] 添加 New API/Sub2API 站点，配置域名、管理员 Key、可选管理员用户 ID。
- [x] 一键校验并导入分组、渠道/账号、模型及关联关系。
- [x] 分组 → 渠道/账号树形展示，标出缺 Key、不可监听和倍率冲突。
- [x] 目标分组必须已存在；删除后暂停写入并告警。

### 3. 上游检测与创建预览

- [x] 上游 Base URL + 普通 Key 去重。
- [x] `/v1/models` 校验模型和自动识别平台。
- [x] Sub2API 通过 `/v1/sub2api/billing` 直接检测倍率。
- [x] New API 通过便宜文本模型请求 + `X-Oneapi-Request-Id` + Key 日志识别倍率。
- [x] 支持“可直接监听 / New API 探测监听 / 仅被动生图 / 缺 Key / 不支持 / 检查失败”状态。
- [x] 只有 Sub2API 直读成功或 New API 实际探测成功的上游可创建；缺 Key、待选探测模型和不支持的平台仅提示，不进入监听列表。
- [x] 自动创建渠道/账号前必须展示平台、模型、目标分组、初始倍率、增减值和监听方式预览。

### 4. 同步引擎

- [x] Sub2API 30–60 秒轮询、New API 日志 10 秒追踪、文本探测每 5 分钟。
- [x] 发现有效变化后立即合并写入；同组并发锁和单写入者约束。
- [x] New API `GroupRatio` 执行读—合并—写，保留其它分组。
- [x] Sub2API 按分组 ID 部分更新 `rate_multiplier`。
- [x] 模型清单每 30 分钟检查，只展示差异、不自动增删目标模型，并在告警后更新比较基线以避免重复提醒。
- [x] 每次探测失败和每次倍率修改保留请求 ID、前后值、时间与结果。

### 5. 管理后台与通知

- [x] FreeModel 风格的概览、站点、上游、同步任务、事件、设置页面。
- [x] 亮色/暗色/跟随系统，桌面、820px、390px 响应式布局。
- [x] loading、empty、error、disabled、success 状态与危险操作确认。
- [x] SSE 网页实时通知。
- [x] 平台统一 SMTP 发件账号，租户独立收件邮箱与开关。
- [x] 默认每 10 分钟合并邮件摘要。

### 6. 验证与交付

- [x] Go 单元测试、`go vet` 与 Linux `-race` 竞态检测通过。
- [x] Vue 类型检查/生产构建通过。
- [x] API 注册、租户鉴权和概览冒烟测试通过。
- [x] 浏览器验证亮/暗主题与 1440/820/390px，390px 无横向溢出且控制台无错误。
- [x] README、环境变量、Docker 部署与连接器兼容说明完整。

## 三、验收证据

| 验收项 | 权威证据 |
| --- | --- |
| 普通 Key 计费探测 | `internal/connectors/source_test.go` 验证 New API request ID 精确匹配与 Sub2API 生效倍率优先级 |
| 目标倍率写入 | `internal/connectors/managed_test.go` 验证 New API 合并保留其它分组且不重建已删除分组；`internal/syncer/engine_test.go` 验证探测、计算、Sub2API 写入闭环 |
| 安全与租户 | `internal/security/security_test.go` 验证加密、密码与会话；`internal/httpapi/server_test.go` 验证注册、鉴权、租户概览，以及前端完整上游检测表单可通过严格 JSON 解码 |
| Go 质量门 | `go vet ./...`、`go test ./...`、Linux 容器 `go test -race ./...` 均通过 |
| 前端质量门 | `npm run typecheck`、`npm run build` 均通过 |
| UI 实机验收 | 浏览器验证亮/暗主题、弹窗 Escape、移动导航、1440/820/390px；最终 390px `scrollWidth = clientWidth = 390`，控制台错误为 0 |
| 部署 | `Dockerfile`、`docker-compose.yml`、`.env.example` 和 `README.md` 已齐备；本机 Docker 构建解析正常，拉取 Node 基础镜像时受外部网络连接阻断 |
| 官方源码真实闭环 | 本地四套独立实例验证 New→New、Sub→Sub、New→Sub、Sub→New 四种倍率写入方向，并验证两种目标的跨平台自动创建渠道/账号；实际结果见 `README.md` |

## 四、已知兼容边界

- 当前官方 Sub2API 永久管理员 Key 使用 `x-api-key`；不同分支可能使用其它 Header，站点配置允许显式指定。
- New API 普通 Key 没有直接倍率接口，必须产生一次最小文本请求并等待该请求日志；无效模型或上游关闭 Key 日志时不可监听。
- 生图规格价格只能在真实流量中观测；当前版本只记录和告警，不自动写入。Sub2API 普通 Key 的计费端点只返回 token 倍率，两种目标平台的生图计费维度也不完全同构。
- 已有渠道/账号的密钥常被目标平台脱敏，系统不会绕过二次验证；用户需要手动补填一次普通 Key。
- Sub2API 关系树会排除明确标记为 OAuth、Setup、Session、Cookie 或凭证文件的账号，只保留 API Key 类型及管理员接口未返回凭证类型、需要人工补 Key 确认的候选账号。
