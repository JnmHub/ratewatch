const KEY="ratewatch-theme",MODES=new Set(["system","light","dark"]);export function readThemeMode(){const v=localStorage.getItem(KEY);return MODES.has(v)?v:"system"}export function applyTheme(mode=readThemeMode()){const m=MODES.has(mode)?mode:"system",resolved=m==="system"?(matchMedia("(prefers-color-scheme: dark)").matches?"dark":"light"):m;document.documentElement.dataset.theme=resolved;document.documentElement.dataset.themeMode=m;return resolved}export function setThemeMode(mode){localStorage.setItem(KEY,mode);return applyTheme(mode)}export function initTheme(){applyTheme();matchMedia("(prefers-color-scheme: dark)").addEventListener?.("change",()=>{if(readThemeMode()==="system")applyTheme()})}
